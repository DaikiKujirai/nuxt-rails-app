export default function () {
  // if (from.path === route.path) {
  // return redirect('/')
  // window.location.href = '/'
  // return new Promise((resolve) => {
  //   console.log(resolve)
  // })
  // }
}
